{
  "name": "cf-worker-toolkit",
  "version": "1.0.0",
  "description": "Módulo para funcionalidades backend compatibles con Cloudflare Workers",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prepare": "npm run clean && npm run build"
  },
  "author": "NeryChi",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  }
}